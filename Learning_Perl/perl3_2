#!/usr/local/perl
	(@lines) = qw(fred betty barney dino Wilma pebbles bamm_bamm);
	@number = <STDIN>;
	chomp(@number);
	
	$end = "0";
	while($end ne $#number+1){
		print $lines[$number[$end]-1]." ";
		$end++;
	}
	print "\n";
